**Avatar** — client or coach. Falls back to initials on the brand gradient when no image.

```jsx
<Avatar name="Jiad Hilal" ring status="online" size={56} />
<Avatar src="/me.jpg" name="Sam R" />
```

`status`: `online` (neon-green glow) · `rest`. `ring` adds the brand purple ring.
