**Badge** — compact status pill (program type, LIVE state, PR, difficulty).

```jsx
<Badge tone="green" variant="solid" dot>Live</Badge>
<Badge tone="purple">Strength</Badge>
```

Tones: `purple | green | red | neutral | dark`. Variants `soft | solid`. `dot` adds a status dot. Labels render UPPERCASE.
