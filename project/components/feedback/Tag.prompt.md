**Tag** — selectable filter chip for browsing programs/exercises (muscle group, equipment).

```jsx
<Tag active onClick={toggle}>Chest</Tag>
<Tag onClick={toggle}>Legs</Tag>
```

`active` fills it with brand purple. Pass `onClick` to make it interactive.
