**ProgressBar** — weekly volume, plan completion, goal progress.

```jsx
<ProgressBar value={68} label="Week 4 of 8" showValue />
```

`tone`: `brand` (gradient) · `green` · `red`. Animates on value change.
