**ProgressRing** — workout completion, daily activity, streak goal.

```jsx
<ProgressRing value={75} sublabel="Today" />
<ProgressRing value={100} tone="green" label="Done" />
```

Center shows % by default; override with `label`. Tones `brand | green | red`.
