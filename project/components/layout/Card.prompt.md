**Card** — the universal surface for content blocks, program tiles, dashboards.

```jsx
<Card variant="surface" interactive onClick={open}>…</Card>
<Card variant="brand">Featured plan</Card>
```

Variants: `surface` (white + shadow) · `brand` (purple gradient + glow) · `dark` · `outline`. Set `interactive` for hover-lift.
