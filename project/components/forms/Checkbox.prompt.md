**Checkbox** — mark a set done, accept terms, toggle filters.

```jsx
<Checkbox label="Set 3 complete" onChange={done => mark(done)} />
```

Controlled via `checked` or uncontrolled via `defaultChecked`.
