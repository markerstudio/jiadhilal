**Select** — choose program, goal, units, difficulty.

```jsx
<Select label="Goal" options={['Build muscle','Lose fat','Get stronger']} />
```

Matches Input styling. `options` accepts `string[]` or `{value,label}[]`. Supports `hint`, `error`, `disabled`.
